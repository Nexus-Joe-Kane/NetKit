SUPPORTWIZARD NETKIT — ENVIRONMENT VARIABLE NAMES

Generated from the source by `npm run env:list`. `npm run check:env` fails
the build if the code reads a name this file does not list, so it cannot
drift. Nothing here is a secret — these are the names of the boxes.

  (!)  removing this loses data or stops the app booting
  (1)  read only when the first admin account is created — safe to delete
  (-)  documented but nothing reads it yet — setting it now does nothing

--- REQUIRED ----------------------------------------------------------

(!) DATA_DIR
    NODE_ENV
(!) SESSION_SECRET

--- RECOMMENDED -------------------------------------------------------

    PORT
    PUBLIC_DIR
    PUBLIC_URL

--- FIRST RUN ONLY ----------------------------------------------------

(1) ADMIN_EMAIL
(1) ADMIN_NAME
(1) ADMIN_PASSWORD

--- CREDENTIALS (settable in the portal instead) ----------------------

    BT_HOME_NETWORK_KEY
    BT_IMEI_LOOKUP_KEY
    BT_LOCATION_INSIGHTS_KEY
    COMPANIES_HOUSE_API_KEY
    DOWNDETECTOR_CLIENT_ID
    DOWNDETECTOR_CLIENT_SECRET
    GIACOM_CLIENT_ID
    GIACOM_CLIENT_SECRET
    ITGLUE_API_KEY
    ITGLUE_BASE_URL
    JOLA_API_KEY
    JOLA_SECRET_KEY
    OFCOM_BROADBAND_API_KEY
    OFCOM_MOBILE_API_KEY
    OPENCELLID
    OPENCELLID_API_KEY
    OPENCELLID_TOKEN
    OS_PLACES_API_KEY
    RESEND_API_KEY
    RESEND_FROM_EMAIL
    THINKBROADBAND_API_KEY
    UNIFI_API_KEY
    UNIFI_INTEGRATION_KEY
    ZENDESK_API_TOKEN
    ZENDESK_EMAIL
    ZENDESK_SUBDOMAIN
    ZEN_CLIENT_ID
    ZEN_CLIENT_SECRET

--- BEHAVIOUR (all have defaults) -------------------------------------

    APP_VERSION
    AVAILABILITY_DAILY_BUDGET
    CACHE_TTL_SECONDS
    CORS_ORIGINS
    POSTCODES_IO_ENABLED
    RATE_LIMIT_MAX
    RATE_LIMIT_WINDOW_MS
    REQUEST_TIMEOUT_MS
    SELFTEST_ON_BOOT
    SUPERVISOR_ENABLED
    SUPERVISOR_ESCALATE_AFTER_MINUTES
    SUPERVISOR_INTERVAL_SECONDS
    ZEN_ALLOW_ORDERING

--- ENDPOINTS (all have defaults) -------------------------------------

    BT_TOKEN_URL
    COMPANIES_HOUSE_BASE_URL
    DOWNDETECTOR_BASE_URL
    DOWNDETECTOR_STATUS_PATH
    DOWNDETECTOR_TOKEN_URL
    GIACOM_BASE_URL
    GIACOM_ENVIRONMENT
    GIACOM_QUALIFICATION_PATH
    GIACOM_SCOPES
    GIACOM_TOKEN_URL
    JOLA_BASE_URL
    OFCOM_BROADBAND_BASE_URL
    OFCOM_BROADBAND_DATASET_PATH
    OFCOM_DATASET_PATH
    OFCOM_MOBILE_BASE_URL
    OPENCELLID_BASE_URL
    OPENCELLID_RADIUS_METRES
    OPENCELLID_SEARCH_PATH
    OS_PLACES_BASE_URL
    POSTCODES_IO_BASE_URL
    RESEND_FROM_NAME
    THINKBROADBAND_AVAILABILITY_PATH
    THINKBROADBAND_BASE_URL
    THINKBROADBAND_KEY_IN_QUERY
    UNIFI_BASE_URL
    ZEN_ASSURANCE_BASE_URL
    ZEN_SCOPES
    ZEN_SELF_SERVICE_BASE_URL
    ZEN_TOKEN_URL

79 variables.

OPENCELLID, OPENCELLID_API_KEY and OPENCELLID_TOKEN are three names for
the same key. Set one. If more than one is set, the first of those three
wins and the others are ignored.
