{
  "name": "supportwizard-netkit",
  "version": "1.0.0",
  "private": true,
  "description": "SupportWizard NetKit \u2014 UK signal, broadband availability, UPRN and line intelligence from a single search box.",
  "workspaces": [
    "shared",
    "server",
    "web"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "npm run dev --workspace=server & npm run dev --workspace=web",
    "dev:server": "npm run dev --workspace=server",
    "dev:web": "npm run dev --workspace=web",
    "build": "npm run build --workspace=shared && npm run build --workspace=server && npm run build --workspace=web",
    "start": "npm run start --workspace=server",
    "typecheck": "tsc -b shared server && tsc --noEmit -p web",
    "test": "npm run test --workspace=shared && npm run test --workspace=server",
    "lint": "npm run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
